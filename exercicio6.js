const respostaDoUsuario = prompt("Digite o número que você quer testar")
const numero = Number(respostaDoUsuario)

if (numero % 2 === 0) {
  console.log("Passou no teste.")
} else {
  console.log("Não passou no teste.")
}

//* O codigo realiza um teste  para saber se o número digitado é par ou impar, e imprime uma mensagem de acordo  com o resultado.  *//

//* Ele imprime  a mensagem "Passou no teste" se o número for par. *//

//*  Ele imprime a mensagem "Não passou no teste" se o número for impar. *//

